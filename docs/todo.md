**TODOS**
- Let each display register at /display and then control what to see on these displays from a new view at /admin/show/displays
- When a new device first visit /display a unique UUID is requested from backend and then stored in localStorage.
- In admin view the admin can name the display device to something appropriate then select what to show on that display (a specific ring or overview for day x)
